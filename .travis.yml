sudo: false
dist: xenial
language: python
python: "3.7"
install:
  - travis_retry python -m pip install -U pip setuptools
  - travis_retry python -m pip install -U black_nbconvert
script:
  - black_nbconvert --check .
